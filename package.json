{
	"name": "@fal-works/sagmal",
	"version": "0.1.0",
	"description": "CLI for transformation using DeepL API",
	"type": "module",
	"bin": {
		"sagmal": "./dist/bin.js"
	},
	"sideEffects": false,
	"files": [
		"dist/"
	],
	"scripts": {
		"build": "tsdown",
		"lint": "biome check",
		"lint:fix": "biome check --fix",
		"check:types": "tsc --noEmit -p tsconfig.json",
		"check": "npm run check:types && npm run lint",
		"fix": "npm run check:types && npm run lint:fix",
		"prebuild": "rimraf -g dist/*",
		"prepublishOnly": "npm run check:types && npm run lint && npm run test"
	},
	"keywords": [
		"translation"
	],
	"author": "FAL",
	"license": "Apache-2.0",
	"engines": {
		"node": ">=22.0.0"
	},
	"dependencies": {
		"deepl-node": "^1.19.0",
		"dotenv": "^17.2.1"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.1.3",
		"@fal-works/deps-docs": "^0.1.0",
		"@types/node": "^24.1.0",
		"rimraf": "^6.0.1",
		"tsdown": "^0.13.0",
		"typescript": "^5.8.3"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/fal-works/sagmal.git"
	},
	"homepage": "https://github.com/fal-works/sagmal#readme"
}
